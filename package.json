{
  "name": "gymdemo",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler pages deploy dist --project-name=gym-website-demo",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.2.2"
  }
}